# This is the environment configuration for the full Docker deployment.
# The `start.sh` script will copy this file to `.env` if it doesn't exist.

# PostgreSQL Credentials for Docker
POSTGRES_USER=urbex_user
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=urbex_db

# The connection string for the DOCKER environment.
# It uses the Docker service name 'db'.
DATABASE_URL="postgresql://urbex_user:your_password_here@db:5432/urbex_db?schema=public"