{
  "name": "urbex-map",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "private": true,
  "license": "ISC",
  "type": "commonjs",
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "concurrently": "^9.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "react-hot-toast": "^2.6.0",
    "sanitize-html": "^2.17.0",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7"
  },
  "scripts": {
    "install:all": "npm install",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "dev": "concurrently \"npm:dev:client\" \"npm:dev:server\"",
    "docker:up": "sudo docker compose up -d",
    "docker:down": "sudo docker compose down",
    "docker:logs": "sudo docker compose logs -f app",
    "docker:rebuild": "sudo docker compose down -v && sudo docker compose build --no-cache && sudo docker compose up -d"
  },
  "devDependencies": {
    "@types/sanitize-html": "^2.16.0",
    "esbuild": "^0.25.9"
  }
}
